node_modules/
dist/
coverage/
.crux/
.crux-axi/
.DS_Store
*.log
package-lock.json

# Never commit registry credentials. This repo is public and a project-level
# .npmrc is exactly where an auth token would land by accident.
.npmrc
